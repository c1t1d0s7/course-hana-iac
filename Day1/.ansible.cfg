[defaults]
inventory = ~/inven.ini ; ansible -i ~/inven.ini
remote_user = vagrant ; ansible -u vagrant
ask_pass = false ; ansible -k

[privilege_escalation]
become = false ; ansible -b
become_method = sudo
become_user = root
become_ask_pass = false ; ansible -K
